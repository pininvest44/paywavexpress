document.getElementById("sendBtn").addEventListener("click",async()=>{const numbers=document.getElementById("numbers").value.split("\n").map(n=>n.trim()).filter(Boolean);const amount=document.getElementById("amount").value;const reference=document.getElementById("reference").value;const response=await fetch("/api/bulk-stk",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({numbers,amount,reference})});const data=await response.json();document.getElementById("status").innerText=`Completed: ${data.total}`;});